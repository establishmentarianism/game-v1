# Название Проекта (RPG Game)

Этот проект создан на игровом движке **Godot 4** (GDScript).

## 📋 Системные требования

Для работы автоматизации (линтер, форматтер, настройка окружения) в VS Code вам понадобятся:

1.  **Python 3.10+**

    - Обязательно при установке поставьте галочку **"Add Python to PATH"** (Добавить в переменные среды).
    - _Зачем:_ Нужен для работы `gdtoolkit` (форматирование и проверка кода).

2.  **PowerShell 7 (Core)**
    - Скрипты автоматизации настроены на использование `pwsh`. Старый Windows PowerShell не поддерживается из-за проблем с кодировкой.
    - [Скачать PowerShell 7](https://github.com/PowerShell/PowerShell/releases) или установить через `winget install Microsoft.PowerShell`.

---

## 🛠 Настройка рабочего окружения

В этом репозитории **нет** жестко прописанных путей к движку. Это сделано для того, чтобы проект работал на любой операционной системе (Windows / Linux / macOS) без конфликтов.

Каждый разработчик настраивает свой редактор самостоятельно.

### 1. Установка расширения

Для работы с кодом в VS Code установите официальное расширение:

- **Godot Tools** (tuchlik)

### 2. Инициализация проекта

После клонирования репозитория откройте его в VS Code и запустите задачу настройки:

1. Нажмите `Ctrl + Shift + P` (или `F1`).
2. Введите **Tasks: Run Task**.
3. Выберите **🛠️ Init Dev Env**.

Это создаст виртуальное окружение Python и установит линтер/форматтер.

### 3. Настройка VS Code (Глобально)

Чтобы VS Code мог запускать Godot, правильно форматировать код и **скрывать системные файлы (.uid)**, добавьте настройки в ваш файл конфигурации.

1. В VS Code нажмите `F1` (или `Ctrl + Shift + P`).
2. Введите: `Open User Settings (JSON)`.
3. Вставьте (или объедините) следующие настройки:

```json
{
  // =========================================================
  // 1. ПУТЬ К ДВИЖКУ (Укажите путь к ВАШЕМУ exe-файлу)
  // =========================================================
  // Windows (Обратите внимание на двойные слеши):
  "godotTools.editorPath.godot4": "ДИСК:\\ПУТЬ_К\\Godot\\Godot_ВЕРСИЯ.exe",

  // macOS / Linux:
  // "godotTools.editorPath.godot4": "/Applications/Godot.app/Contents/MacOS/Godot",

  "godot_tools.lsp_port": 6005,

  // =========================================================
  // 2. ГИГИЕНА КОДА (Code Style)
  // =========================================================
  "editor.formatOnSave": true,
  "files.trimTrailingWhitespace": true,
  "files.insertFinalNewline": true,
  "files.trimFinalNewlines": true,

  // =========================================================
  // 3. СКРЫТИЕ ФАЙЛОВ .UID (File Nesting)
  // =========================================================
  // Включает группировку файлов.
  // ВАЖНО: В проводнике VS Code сортировка должна стоять по ИМЕНИ (Sort by Name), иначе это не сработает!
  "explorer.fileNesting.enabled": true,
  "explorer.fileNesting.expand": false, // По умолчанию свернуто
"explorer.fileNesting.patterns": {
    "*.gd": "$(capture).gd.uid, $(capture).test.gd",
}
  // =========================================================
  // 4. НАСТРОЙКИ GODOT (Только для GDScript)
  // =========================================================
  "[gdscript]": {
    "editor.insertSpaces": false, // Godot требует TAB
    "editor.tabSize": 4,
    "editor.inlayHints.enabled": "on"
  },

  // =========================================================
  // 5. ВИЗУАЛ
  // =========================================================
  "editor.bracketPairColorization.enabled": true,
  "editor.guides.bracketPairs": true
}
```

> **Важно:** После изменения настроек **полностью перезапустите VS Code**.

## 🚀 Как запустить проект

### Вариант 1: Через редактор Godot

1. Откройте Godot.
2. Нажмите **Import**.
3. Выберите файл `project.godot` в корневой папке.

### Вариант 2: Через VS Code

Если вы выполнили настройку выше:

1. Откройте любой `.gd` файл.
2. Нажмите иконку Godot (справа сверху) -> **Open Godot Editor**.
3. Или нажмите `F5` для запуска игры с отладчиком.
