# Руководство разработчика

Здесь описано, как настроить рабочее окружение, чтобы линтер ругался по делу, код форматировался сам, а тесты запускались одной кнопкой.

## 📋 Системные требования

Для работы скриптов автоматизации вам понадобятся:

1.  **Python 3.10+**
    - Обязательно добавьте в PATH при установке.
    - Используется для работы инструментов `gdtoolkit` и менеджера `uv`.
2.  **PowerShell 7 (Core)**
    - Скрипты написаны для `pwsh`. Старый Windows PowerShell может работать некорректно.
3.  **VS Code + Расширение "Godot Tools"**

---

## 🛠 Автоматическая настройка (Bootstrap)

Вам не нужно вручную ставить библиотеки Python. Мы используем менеджер пакетов `uv` (он быстрее pip в 100 раз), но даже его ставить вручную не обязательно.

1.  Откройте проект в VS Code.
2.  Нажмите `Ctrl + Shift + B` (или `F1` -> **Tasks: Run Task**).
3.  Выберите **🛠️ Init Dev Env**.

> Скрипт сам проверит наличие `uv`, установит его при необходимости, создаст виртуальное окружение `.venv` и скачает `gdlint` и `gdformat`.

---

## ⚙️ Настройка VS Code

Чтобы редактор понимал специфику Godot (скрывал `.uid` файлы, знал путь к движку), добавьте эти настройки в свой **User Settings (JSON)** или **Workspace Settings**.

1.  Нажмите `F1` -> `Open User Settings (JSON)`.
2.  Добавьте/Объедините с вашим конфигом:

```json
{
  // ---------------------------------------------------------
  // 1. ПУТЬ К ДВИЖКУ (ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ ПОД СЕБЯ!)
  // ---------------------------------------------------------
  // Без этого не будет работать запуск тестов и сцен из VS Code.
  "godotTools.editorPath.godot4": "C:\\Games\\Godot\\Godot_v4.5.1-stable_win64.exe",
  "godot_tools.lsp_port": 6005,

  // ---------------------------------------------------------
  // 2. ГИГИЕНА КОДА (Format on Save)
  // ---------------------------------------------------------
  "editor.formatOnSave": true,
  "files.trimTrailingWhitespace": true,
  "files.insertFinalNewline": true,
  "[gdscript]": {
    "editor.insertSpaces": false, // Godot требует TAB
    "editor.tabSize": 4
  },

  // ---------------------------------------------------------
  // 3. ЧИСТОТА ПРОВОДНИКА (File Nesting)
  // ---------------------------------------------------------
  // Скрывает служебные файлы Godot внутри основных.
  // ВАЖНО: В проводнике сортировка должна быть по ИМЕНИ.
  "explorer.fileNesting.enabled": true,
  "explorer.fileNesting.expand": false,
  "explorer.fileNesting.patterns": {
    "*.gd": "$(capture).gd.uid, $(capture).tscn, $(capture).test.gd",
    "*.tscn": "$(capture).tscn.uid",
    "*.png": "$(capture).png.import",
    "*.wav": "$(capture).wav.import",
    "*.ogg": "$(capture).ogg.import"
  }
}
```
