{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "🛠️ Init Dev Env",
      "type": "shell",
      // Используем pwsh явно. Это позволяет передать файл скрипта как аргумент.
      "command": "pwsh",
      "args": [
        "-ExecutionPolicy",
        "Bypass",
        "-File",
        ".\\dev_tools\\setup.ps1"
      ],
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      },
      "problemMatcher": []
    },
    {
      "label": "🎨 Format Code",
      "type": "shell",
      "command": "uv",
      "args": ["run", "gdformat", "src", "tests"],
      "problemMatcher": []
    },
    {
      "label": "🔍 Lint Code",
      "type": "shell",
      "command": "uv",
      "args": ["run", "gdlint", "src", "tests"],
      "problemMatcher": []
    },
    {
      "label": "✨ Lint & Format",
      "type": "shell",
      // Эта задача ничего не делает сама, она запускает две предыдущие
      "dependsOn": ["🎨 Format Code", "🔍 Lint Code"],
      "dependsOrder": "sequence", // Сначала форматируем, потом проверяем
      "problemMatcher": []
    },
    {
      "label": "🧪 Run All Tests",
      "type": "shell",
      // 1. Команда - оператор запуска PowerShell
      "command": "&",
      "args": [
        // 2. Путь к Godot в ОДИНАРНЫХ кавычках (чтобы PS съел пробелы)
        "'${config:godotTools.editorPath.godot4}'",

        // 3. Стандартные флаги Godot
        "--headless",
        "-s",
        "addons/gut/gut_cmdln.gd",

        // 4. ВАЖНЫЕ ФЛАГИ GUT (которые были потеряны)
        "-gdir",
        "res://", // Где искать (корень)
        "-ginclude_subdirs", // <-- ОБЯЗАТЕЛЬНО: искать в подпапках (src/, tests/)
        "-gsuffix",
        ".test.gd", // <-- ОБЯЗАТЕЛЬНО: искать файлы, заканчивающиеся на .test.gd
        "-gprefix=", // Сбрасываем префикс (по умолчанию test_)

        "-gexit" // Выйти после тестов
      ],
      "problemMatcher": "$godot-shader-compiler",
      "group": {
        "kind": "test",
        "isDefault": true
      }
    }
  ]
}
